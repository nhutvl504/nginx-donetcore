machine:
  services:
    - docker
general:
  artifacts:
    - "app"
dependencies:
  post:
    - docker build -t $DEV_AWS_ECR_URI:$CIRCLE_BUILD_NUM .
test:
  pre:
    - aws configure list
  override:
    - echo "done "
deployment:
  prod:
    branch: master
    commands:
      - sh ./push-ecr.sh
